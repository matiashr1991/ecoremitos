version: "3.9"

services:
  app:
    image: ${APP_IMAGE}
    environment:
      TZ: ${TZ}
      APP_ENV: ${APP_ENV}
      PORT: ${APP_INTERNAL_PORT}
      APP_BASE_URL: https://${DOMAIN}

      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${POSTGRES_DB}
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_URL: ${DATABASE_URL}

      BETTER_AUTH_URL: https://${DOMAIN}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}

    networks:
      - web
      - ecoremitos_internal

    volumes:
      - /opt/stacks/ecoremitos/data/uploads:/app/public/uploads
      - /opt/stacks/ecoremitos/data/audit-archive:/app/audit-archive

    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager

      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
        window: 30s

      update_config:
        parallelism: 1
        order: start-first
        failure_action: rollback
        delay: 10s

      rollback_config:
        parallelism: 1
        order: stop-first

      labels:
        - traefik.enable=true
        - traefik.docker.network=web

        - traefik.http.routers.ecoremitos.rule=Host(`${DOMAIN}`)
        - traefik.http.routers.ecoremitos.entrypoints=websecure
        - traefik.http.routers.ecoremitos.tls=true
        - traefik.http.routers.ecoremitos.tls.certresolver=${CERTRESOLVER}

        - traefik.http.routers.ecoremitos.service=ecoremitos-svc
        - traefik.http.services.ecoremitos-svc.loadbalancer.server.port=${APP_INTERNAL_PORT}

  db:
    image: postgres:15-alpine
    environment:
      TZ: ${TZ}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata

    networks:
      - ecoremitos_internal

    volumes:
      - /opt/stacks/ecoremitos/data/postgres:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager

      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
        window: 30s

networks:
  web:
    external: true
    name: web

  ecoremitos_internal:
    driver: overlay
    attachable: true
